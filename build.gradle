plugins {
    id 'base' // To add "clean" task to the root project.
}

subprojects {
    apply from: rootProject.file('common.gradle')
}

// publishing tasks

task('install') {
    dependsOn ':JmePowerLibrary:install', ':LemurPower:install'
    description 'Installs Maven artifacts to the local repository.'
}
task('release') {
    dependsOn ':JmePowerLibrary:release', ':LemurPower:release'
    description 'Stages Maven artifacts to Sonatype OSSRH.'
}
